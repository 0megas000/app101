{
  "name": "preworkout-dispenser-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Smart Pre-Workout Dispenser Platform — kiosk, admin, API, simulated hardware",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace server & npm run dev --workspace web & wait",
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace server && npm run build --workspace web",
    "db:migrate": "npm run db:migrate --workspace server",
    "db:seed": "npm run db:seed --workspace server",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web",
    "test:e2e": "node e2e/smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.1"
  }
}
